name: energy_tracker
description: Energy consumption calculator
publish_to: 'none' # Remove this line if you plan to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^1.1.0
  flutter_lints: ^6.0.0

  # Add any other packages you use, for example:
  # intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Add any other dev packages you use

flutter:
  uses-material-design: true
